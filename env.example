# Django Settings
SECRET_KEY=your-secret-key-here-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (for local development - SQLite)
# For production on Railway, these will be set automatically by Railway

# Database (PostgreSQL - used in production)
# DATABASE_URL will be automatically set by Railway
# If you want to use PostgreSQL locally, uncomment and set these:
# DATABASE_NAME=workshop_manager
# DATABASE_USER=postgres
# DATABASE_PASSWORD=your-password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

